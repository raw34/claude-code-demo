# 本地开发环境配置示例
# 复制此文件为 .env 用于本地开发

# 数据库配置 - 连接到 Docker 容器
DB_HOST=localhost
DB_PORT=3306
DB_USER=userapp
DB_PASSWORD=userpassword
DB_NAME=user_db

# Redis配置 - 连接到 Docker 容器
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redispassword

# MySQL Root密码（Docker Compose使用）
DB_ROOT_PASSWORD=rootpassword

# JWT配置
JWT_SECRET=your-secret-key-here

# 服务器配置
API_PORT=8080
GIN_MODE=debug